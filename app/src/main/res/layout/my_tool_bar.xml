<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:android="http://schemas.android.com/apk/res/android">
    <data>
        <variable name="title" type="androidx.lifecycle.MutableLiveData&lt;String&gt;"/>
        <variable name="callback" type="androidx.lifecycle.MutableLiveData&lt;Boolean&gt;"/>
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:background="@drawable/black_green_gradient"
            android:layout_height="56dp">
        <carbon.widget.TextView
                android:id="@+id/l_title"
                app:text="@{title}"
                app:carbon_fontPath="@string/font_bold"
                android:text="@string/app_name"
                android:padding="0dp"
                android:textSize="22sp"
                android:textColor="@color/carbon_white"
                android:layout_margin="0dp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent" app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintBottom_toBottomOf="parent" app:layout_constraintVertical_bias="0.5"/>
        <carbon.widget.ImageView
                android:clickable="true"
                app:carbon_tint="@color/carbon_white"
                android:focusable="true"
                app:carbon_rippleColor="@color/carbon_black"
                app:carbon_rippleStyle="borderless"
                app:carbon_rippleRadius="5dp"
                app:title="@{title}"
                app:go_back="@{callback}"
                android:layout_width="30dp"
                android:layout_height="30dp"
                app:srcCompat="@drawable/ic_arrow_left_black"
                android:id="@+id/imageView5"
                app:layout_constraintBottom_toBottomOf="@+id/l_title"
                app:layout_constraintTop_toTopOf="@+id/l_title" app:layout_constraintEnd_toStartOf="@+id/l_title"
                app:layout_constraintStart_toStartOf="parent" app:layout_constraintHorizontal_bias="0.0"
                android:layout_marginStart="16dp" android:layout_marginEnd="16dp"
        />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
